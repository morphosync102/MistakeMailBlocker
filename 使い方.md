# 使い方ガイド

## インストール手順

### 1. 拡張機能のビルド（初回のみ）

プロジェクトフォルダで以下のコマンドを実行:

```bash
npm install
npm run build
```

### 2. Chromeに拡張機能をインストール

1. Chromeブラウザで `chrome://extensions/` を開く
2. 右上の **「デベロッパーモード」** を有効にする
3. **「パッケージ化されていない拡張機能を読み込む」** ボタンをクリック
4. `gmail-mistake-blocker/dist` フォルダを選択

### 3. 自社ドメインの設定

`src/utils.ts` ファイルを開いて、以下の部分を編集:

```typescript
const COMPANY_DOMAINS = ['example.com', 'yourcompany.com'];
```

↓ 自社のドメインに変更

```typescript
const COMPANY_DOMAINS = ['mycompany.co.jp', 'mycompany.com'];
```

変更後、再度ビルド:
```bash
npm run build
```

Chrome拡張機能ページで **更新ボタン（🔄）** をクリック

## 使い方

### 基本的な流れ

1. Gmailでメールを作成
2. 送信ボタンをクリック
3. **第1段階: チェック画面** が表示される
   - 送信元のチェック
   - 送信先のチェック（社内/社外）
   - 件名のチェック
   - 添付ファイルのチェック
   - 警告がある場合はチェックボックスで確認
4. **第2段階: 最終確認画面** が表示される
   - メール内容の最終確認
   - 社外向けの場合は **4桁の送信コード** を入力
5. 「送信」ボタンで実際に送信

### チェック内容

#### ✅ 送信元チェック
- 社外向けメールを個人アドレスから送信していないか確認

#### ✅ 送信先チェック
- 社内/社外の宛先を表示
- 社外ドメインの一覧を表示
- 誤った宛先がないか確認

#### ✅ 件名チェック
- 件名が空の場合はエラー表示

#### ✅ 添付ファイルチェック
- 「添付」「attached」「attachment」のキーワードがあるのにファイルがない場合に警告
- 社外向けメールでZIPファイル以外の添付がある場合に警告
- 社外向けZIPファイルのパスワード保護確認を促す

### 時間帯による警告色

最終確認画面のヘッダー色が時間帯によって変化します:

- **🟢 9:00-12:00**: 緑（通常業務時間）
- **🟠 12:00-16:00**: オレンジ（昼休み・午後）
- **🔴 16:00-22:00**: 赤（業務時間外・要注意）
- **🟡 22:00-9:00**: 黄（深夜・早朝・要注意）

## トラブルシューティング

### 拡張機能が動作しない場合

1. Chrome拡張機能ページ (`chrome://extensions/`) で拡張機能が有効になっているか確認
2. Gmailページを再読み込み (F5)
3. ブラウザを再起動

### 送信ボタンをクリックしても何も起こらない

1. Gmailの画面構造が変更された可能性があります
2. ブラウザのコンソール (F12) でエラーを確認
3. 拡張機能を更新してください

### ビルドエラーが出る場合

```bash
# node_modulesを削除して再インストール
rm -rf node_modules package-lock.json
npm install
npm run build
```

## 開発者向け

### 開発モード

```bash
npm run dev
```

ファイル変更を監視して自動的に再ビルドされます。
変更後はChrome拡張機能ページで拡張機能を再読み込みしてください。

### プロジェクト構造

```
gmail-mistake-blocker/
├── src/
│   ├── components/      # Reactコンポーネント
│   │   ├── CheckModal.tsx      # 第1段階チェック画面
│   │   ├── ConfirmModal.tsx    # 第2段階確認画面
│   │   ├── CheckItem.tsx       # チェック項目コンポーネント
│   │   └── ModalManager.tsx    # モーダル管理
│   ├── content/         # コンテントスクリプト
│   │   └── index.tsx           # メインスクリプト
│   ├── types.ts         # 型定義
│   ├── utils.ts         # ユーティリティ関数
│   ├── validator.ts     # バリデーションロジック
│   ├── gmailScraper.ts  # Gmail DOM操作
│   └── styles.css       # スタイル
├── dist/               # ビルド成果物
├── manifest.json       # Chrome拡張機能マニフェスト
└── README.md           # プロジェクト説明
```

## 注意事項

- この拡張機能はGmailのDOM構造に依存しています
- Gmailのアップデートにより動作しなくなる可能性があります
- ZIPファイルのパスワード保護はブラウザ上では確認できないため、警告のみ表示されます
- 重要なメール送信時は必ず内容を確認してください
